/**
 * Trade Routes
 * 
 * TODO: Register trade endpoints
 */

import { FastifyInstance } from 'fastify'
import { TradeController } from '../controllers/tradeController'

export async function tradeRoutes(fastify: FastifyInstance) {
  // TODO: Initialize TradeController with dependencies
  // const tradeController = new TradeController(tradeEngine)

  // TODO: Register routes
  // fastify.post('/api/trades', async (request, reply) => {
  //   return tradeController.executeTrade(request, reply)
  // })

  // fastify.get('/api/trades/:userId', async (request, reply) => {
  //   return tradeController.getTradeHistory(request, reply)
  // })
}

