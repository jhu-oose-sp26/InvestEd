/**
 * Portfolio Routes
 * 
 * TODO: Register portfolio endpoints
 */

import { FastifyInstance } from 'fastify'
import { PortfolioController } from '../controllers/portfolioController'

export async function portfolioRoutes(fastify: FastifyInstance) {
  // TODO: Initialize PortfolioController with dependencies
  // const portfolioController = new PortfolioController(portfolioService)

  // TODO: Register routes
  // fastify.get('/api/portfolio/:userId', async (request, reply) => {
  //   return portfolioController.getPortfolio(request, reply)
  // })

  // fastify.get('/api/leaderboard', async (request, reply) => {
  //   return portfolioController.getLeaderboard(request, reply)
  // })
}

